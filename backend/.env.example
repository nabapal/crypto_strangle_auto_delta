# Delta Strangle Backend Environment

APP_NAME=Delta Strangle Control Plane
DATABASE_URL=sqlite+aiosqlite:///../data/delta_trader.db
DELTA_API_KEY=
DELTA_API_SECRET=
ALLOWED_ORIGINS=http://localhost:5173
DEBUG_HTTP_LOGGING=false
DELTA_DEBUG_VERBOSE=false
DELTA_LIVE_TRADING=false

# Backend environment variables are defined in the repository root template.
# Copy the `# Backend` section from ../.env.example into backend/.env before running the service.
