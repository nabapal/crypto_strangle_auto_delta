# Environment template for the Delta Strangle enterprise stack
# Copy the relevant sections into backend/.env and frontend/.env (or export
# them in your shell) before running the services.

########################################
# Backend (FastAPI)
########################################
APP_NAME="Delta Strangle Control Plane"
DATABASE_URL=sqlite+aiosqlite:///../data/delta_trader.db
# UID/GID used for backend containers during docker builds.
APP_UID=1000
APP_GID=1000
DELTA_API_KEY=
DELTA_API_SECRET=
# Provide explicit origins for CORS (avoid "*"). Include every URL you'll load the UI from.
ALLOWED_ORIGINS=["http://localhost","http://127.0.0.1","http://localhost:5173"]
# Use lowercase true/false to satisfy pydantic boolean parsing.
DEBUG_HTTP_LOGGING=false
DELTA_DEBUG_VERBOSE=false
DELTA_LIVE_TRADING=false
# Structured logging configuration
LOG_LEVEL=INFO
ENGINE_DEBUG_SAMPLE_RATE=5
TICK_LOG_SAMPLE_RATE=50
LOG_INGEST_API_KEY=
LOG_INGEST_MAX_BATCH=100
BACKEND_LOG_INGEST_ENABLED=true
BACKEND_LOG_PATH=logs/backend.log
BACKEND_LOG_POLL_INTERVAL=1.0
BACKEND_LOG_BATCH_SIZE=128
BACKEND_LOG_RETENTION_DAYS=7

########################################
# Frontend (Vite)
########################################
# Include the /api suffix so React Query calls resolve correctly.
VITE_API_BASE_URL=http://localhost:8001/api
# Enable verbose request/response logging in the browser console.
VITE_ENABLE_API_DEBUG=false
# Remote log ingestion settings (defaults keep telemetry local unless you opt in).
VITE_ENABLE_REMOTE_LOGS=false
VITE_LOG_ENDPOINT=/api/logs/batch
VITE_LOG_API_KEY=
VITE_LOG_DEDUP_WINDOW=1000
VITE_LOG_DEDUP_THRESHOLD=5
# Delta Exchange WebSocket endpoint for live quotes.
VITE_DELTA_WEBSOCKET_URL=wss://socket.delta.exchange
